{
  "name": "fost",
  "version": "0.1.0",
  "description": "Framework for Optimized SDK generation using Type analysis",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "jest",
    "lint": "eslint src",
    "cli": "node dist/cli/index.js"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
